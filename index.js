'use static';

// 回答数管理
let flag = 0

let qnum = ` ${flag + 1} `;
let qlabel = `第${qnum}問`;
//console.log(qlabel);

// 書き換え要素の コンストラクタ

// イニシャライズ _待機画面(一問目)_

// イニシャライズから if で丸めて分岐を管理？


/* dom操作用関数

# id 取得
let element = document.getElementById(id);

# id テキスト
## 文字列そのもの
let text = element.textContent;
element.textContent = "this is some sample text";

## タグも含めて
const content = element.innerHTML;
element.innerHTML = htmlString;

> 文書に HTML を挿入するという場合には、`insertAdjacentHTML()`メソッドを使用

*/
